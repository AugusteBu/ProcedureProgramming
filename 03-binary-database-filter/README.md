## Files in this folder
* generator.c: The code that creates the mock data.
* query1.c, query2.c, etc.: The code where I change the filter conditions.
* expected_output.txt: A text file with the correct results for different queries (like Query 1, Query 2, etc.) so I can check if my code output matches.